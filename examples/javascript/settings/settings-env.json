{
  "_env": {
    "CONFIG_PLATFORM": "specialCrm",
    "CONFIG_COMPUTE": "restApi",
    "node_env readme": "The NODE_ENV should be set in the shell via export NODE_ENV='dev'. Setting it here only for demo purposes.",
    "NODE_ENV": "dev"
  }
}
